# Plano de fatia — v1.11.0a8b67 — classificacoes-pessoa

Escrito pelo orquestrador a partir do inventário
`docs/arquitetura/debate/09-inventario-classificacoes-pessoa.md`. Decisões em
`docs/arquitetura/DECISOES.md`: D65 (seletor entra junto com o cadastro), D68 (módulo, rota, menu),
D69 (sem reativar), D70 (seletor no Cliente). Sem quarteto: a forma veio da D65 e o molde é
Condições de Pagamento, em produção.

## 0. Contrato da fatia (legível por máquina)

```yaml
slice:
  id: v1.11.0a8b67
  module: Pessoas, Clientes
  feature: Cadastro de Classificações de Pessoa e seletor no Cliente
  item_do_plano: docs/PLANO-FRONTEND-ONDA-OPERACAO.md §b67 (D65, D68–D70)

regime: implementacao

risk: HIGH
accessRisk: NENHUM        # só abre acesso a uma tela que não existia (D68)

requirements:
  contractChange:        true    # 3 requests novos (criar, atualizar, inativar) e o response do catálogo
  permissionChange:      true    # regra de rota nova, guard de ações, guarda por campo no Cliente
  moneyOnScreen:         false
  uiPattern:             false   # molde Condições de Pagamento
  organizationalContext: true    # catálogo por empresa: query key e enabled por empresaId
  navigationChange:      true    # rota e item de menu novos
  structuralGate:        true    # gate de contratos de request passa a cobrir os 3 records
  platformChange:        false
  newScreen:             true

graphNodes: [design, builder, gate_estrutural, tests, e2e, qa_review]

testPlan:
  UNIT: required
  COMPONENT: required             # página (inativar, código imutável) e seletor do Cliente
  E2E_MOCKADO: required           # rota, menu, sessão nominal
  CONTRATO: required
  PERMISSAO: required
  CONTEXTO_ORGANIZACIONAL: required
  GATE_ESTRUTURAL: required
  REGRESSAO_TEXTUAL: false

classeDeDefeito:
  o_que_esta_fatia_abre: >
    item de menu novo cujo pai, regra de rota ou guarda não contam a mesma
    história — some o grupo inteiro de quem tem direito, ou abre tela que o
    backend recusa (ILUSAO). E PUT que substitui nome e descrição: descrição
    omitida apaga.
  gate_existente: >
    validate:guard-permission-map (C1, C2, C3) para menu e rota; gate de
    contratos de request estendido; testes estruturais que leem AppMenu.tsx e
    routePermissions.ts.
```

## 1. Objetivo

Quem cadastra pessoas passa a manter a tabela de classificações da empresa (criar, renomear,
descrever, inativar) numa tela própria em Cadastros, e a escolher a classificação do cliente na aba
"Comercial" em vez de ela só ser preservada.

## 2. Leitura base (todo agente desta fatia)

- `docs/arquitetura/debate/09-inventario-classificacoes-pessoa.md` — endpoints, campos, limites,
  semântica de gravação, permissões e o que cada gate cobra, com arquivo e linha.
- `docs/arquitetura/DECISOES.md`: D65, D66, D68, D69, D70.
- Molde: `features/financeiro/components/CondicoesPagamentoPage.tsx`,
  `CondicaoPagamentoFormDialog.tsx`, `app/(main)/financeiro/condicoes-pagamento/page.tsx`,
  `lib/security/routePermissions.ts:35`, `layout/AppMenu.tsx:110,117`.
- Seletor com guarda por campo já em produção: `features/clientes/components/ClienteFormDialog.tsx`
  (`comValorGravadoNeutro`, aviso único da aba).

## 3. Decisões já tomadas (o agente NÃO redecide)

| Id | Decisão |
| --- | --- |
| D65 | O seletor de `classificacaoId` entra junto com o cadastro |
| D66 | Guarda de catálogo por campo, rótulo neutro, um aviso por aba, valor gravado segue no `PUT` |
| D68 | Código em `features/pessoas/`; rota `/pessoas/classificacoes` com regra própria antes da genérica, `anyOf: ['PESSOAS_CONSULTAR']`; item "Classificações de pessoa" no grupo Cadastros com `['PESSOAS_CONSULTAR']`; gravar exige `CLASSIFICACOES_PESSOA_GERENCIAR` |
| D69 | Inativa visível, sem editar nem inativar; o diálogo de inativação avisa que não há reativação pela aplicação |
| D70 | Seletor por empresa; opções ativas, mais a inativa gravada marcada "(inativa)" |

Quem discorda devolve `needs_decision` com o id e o caso concreto.

## 4. Armadilhas conhecidas desta fatia

- **Menu e rota contam a história juntos.** O pai "Cadastros" já contém `PESSOAS_CONSULTAR`; se o
  item filho usar outra permissão, o pai precisa conter também, senão o grupo some para quem tem
  direito (C2). A regra de rota nova vem **antes** da genérica `/pessoas`, senão nunca casa (C3).
- **O literal `'CLASSIFICACOES_PESSOA_GERENCIAR'` precisa existir dentro de `features/pessoas/`**
  (guard das ações), senão C1 acusa chamada sem guarda e estoura o teto fechado do allowlist, que
  não se edita à mão.
- **Testes estruturais leem `AppMenu.tsx` e `routePermissions.ts` como texto** — há dezenas
  (`grep -rln "AppMenu\|routePermissions" tests/unit tests/components`), inclusive a prova do total
  de itens do menu. Todo item de menu novo mexe neles; o recorte de testes os inclui. Foi um teste
  desse tipo, fora do recorte, que derrubou o CI da #28.
- **`PUT` substitui nome e descrição.** O formulário de edição parte do registro gravado; `descricao`
  vazia vai `null` só quando o operador apagou.
- **Código imutável e normalizado.** Desabilitado na edição (molde `CondicaoPagamentoFormDialog.tsx:52`);
  o backend normaliza para maiúsculas e recusa espaço interno — a validação do formulário reflete, a
  regra continua do backend.
- **Catálogo por empresa.** `GET` exige `empresaId`; query key e `enabled` por empresa, como
  `condicoesPagamentoQueryKey(empresaId)`.
- **Escopo de escrita.** `tests/**` e `scripts/**` são do `engenheiro-testes`; o `dev-senior-react`
  só roda os comandos de geração do ritual de versão.

## 5. Critérios de aceite

| Id | Critério | Como se verifica |
| --- | --- | --- |
| AC-1 | Listagem de classificações da empresa do contexto, ativas e inativas com `Tag` de status, filtro local por código/nome | componente + E2E |
| AC-2 | Criar: código (máx. 40, sem espaço interno), nome (máx. 120), descrição (máx. 300); payload com `empresaId` | unitário de schema + componente |
| AC-3 | Editar: código desabilitado; `PUT` com nome e descrição do registro, `null` só se apagada | componente (payload) |
| AC-4 | Inativar: `ReasonDialog` com motivo obrigatório e o aviso de que não há reativação pela aplicação; `POST .../inativar` com `{ empresaId, motivo }` | componente |
| AC-5 | Classificação inativa: editar e inativar desabilitados | componente |
| AC-6 | Sessão com `PESSOAS_CONSULTAR` sem `CLASSIFICACOES_PESSOA_GERENCIAR`: vê a tela e o menu; "Nova classificação" desabilitado com tooltip do motivo; ações de linha não disponíveis (o `DataTableActions` as oculta, comportamento em produção em todo o app — aceito como na `b66`) | E2E, sessão nominal |
| AC-7 | Sessão com só `CLASSIFICACOES_PESSOA_GERENCIAR`: não vê o item de menu e a rota não abre (D68) | E2E, sessão nominal |
| AC-8 | Aba "Comercial" do Cliente: seletor de classificação com as ativas da empresa; escolher grava `classificacaoId` no `PUT .../configuracao-comercial` | componente (payload) + E2E |
| AC-9 | Valor gravado de classificação inativa aparece como "(inativa)" e é reenviado sem alteração | componente |
| AC-10 | Sem `PESSOAS_CONSULTAR`: seletor desabilitado, rótulo neutro, a permissão entra no aviso único da aba, valor reenviado | E2E, sessão nominal |
| AC-11 | Gate de contratos de request cobre os 3 records; prova vermelha nominal com campo removido | `tests/unit/gateContractRequestFields.test.ts` |
| AC-12 | `validate:guard-permission-map`, `validate:backend-contract-map`, `validate:backend-permissions` verdes sem entrada nova em allowlist | comandos |

## 6. Blocos de trabalho

### Bloco 0 — `designer-ux-erp` — nó `design` (antes do builder)

- **Entrega**: especificação no próprio relatório, não arquivo — o componente é escrito no Bloco A.
  Sobre o molde de Condições de Pagamento: colunas da listagem, ordem e rótulos do formulário, texto
  do aviso de inativação (D69), rótulo do item de menu e ícone, posição do seletor na aba
  "Comercial" do Cliente, e os sete estados de tela da página nova. Confira no código (e no preview,
  se autenticar for possível sem credencial — se não for, diga) em vez de supor.
- **Não escreve arquivo.** O que ele devolve entra no bloco do builder.

### Bloco A — `dev-senior-react` — tipos, schemas, API, hooks, tela, rota, menu, seletor, ritual

- **Arquivos**: `features/pessoas/**` (novos: tipos, schemas, api, hooks, componentes da página),
  `app/(main)/pessoas/classificacoes/page.tsx`, `lib/security/routePermissions.ts`,
  `layout/AppMenu.tsx`, `features/clientes/components/ClienteFormDialog.tsx` (seletor),
  `components/feedback/ReasonDialog.tsx` (só o prop opcional `warning`, aditivo — decidido pelo
  orquestrador sobre a especificação do nó `design`, que mostrou que o componente não tinha onde
  exibir o aviso da D69), e o ritual
  de versão `1.11.0a8b67` (mesmos arquivos do commit da `b66`; artefatos gerados só por comando).
- **O que NÃO tocar**: `tests/**`, `scripts/**`, `CHANGELOG.md`, as permissões do molde Condições de
  Pagamento (achado da D68, fora do recorte).
- **Como saber que terminou**: `validate:source`, `typecheck`, `lint`, `validate:guard-permission-map`,
  `validate:backend-contract-map`, `validate:backend-permissions` verdes (`validate:ci` fica vermelho
  só pelo CHANGELOG até o Bloco E).

### Bloco B — `engenheiro-testes` — gate, testes, spec E2E

- Gate primeiro: os 3 records em `SCHEMA_TO_REQUEST_MAP`/`recordNames`, **parser intacto**; divergência
  vinda do texto do schema volta como `needs_decision` (lição da `b66`). Prova vermelha nominal.
- Recorte obrigatório: os testes novos, **mais todo teste que lê** `layout/AppMenu.tsx`,
  `lib/security/routePermissions.ts`, `features/pessoas/` ou `features/clientes/` como texto
  (`grep -rln`), rodados com `npx vitest run` — nunca `npm run test:unit`.
- Componente no padrão `ProdutosPageAC2AC6` (mutações reais, só a API mockada). Spec E2E mockada para
  AC-1, AC-6, AC-7, AC-8, AC-10, provada com `--list` no config isolado.

### Bloco C — `devops-frontend` — execução do E2E

- Mesma receita da `b66`: porta 3411 livre antes, `logosoft-e2e-isolated`, identidade conferida,
  duas execuções, servidor parado ao fim.

### Bloco E — `dev-senior-react` — `CHANGELOG.md`

- Entrada `# v1.11.0a8b67` com os números medidos pelos Blocos B e C.

### Bloco D — `qa-revisor`

- Diff contra os blocos; todos os gates; a história de permissão contada igual em union, catálogo,
  rota, menu e guarda.

## 7. Sequenciamento

`design` (0) → `builder` (A) → `gate_estrutural` (B, gate primeiro) → `tests` (B) → `e2e` (C) →
changelog (E) → `qa_review` (D). O `inventario` já rodou.

## 8. Fora do escopo, nominalmente

| O quê | Destino |
| --- | --- |
| Reativar classificação | B-13 (D69) |
| Ilusão de acesso no molde de Condições de Pagamento (rota/menu aceitam gerenciar sem consultar) | achado registrado na D68; fatia própria com classificação de `accessRisk` |
| Condições de Pagamento fora do gate de contratos de request | achado do inventário §6; fatia própria |
| Classificação em Fornecedor ou Pessoa | não há campo no contrato do recorte |

## 9. Estado da execução

```yaml
noCorrente: release
nosConcluidos: [inventario, design, builder, gate_estrutural, tests, e2e, changelog, qa_review]
# e2e: execução 5 — 5/5 nas duas, servidor isolado 3411. QA: 1º veredito BLOQUEADO por 9 erros
# de typecheck nos testes de componente novos (o agente de testes não rodava typecheck); corrigidos
# pelo engenheiro-testes (o QA tinha indicado o dev-senior-react, bloqueado em tests/**) sem mudar
# asserção. 2º veredito: APROVADO.
# e2e: execução 1: 3/5 (AC-7 esperava o texto interno da página, mas quem bloqueia é o
# RoutePermissionGate; AC-10 com toBeDisabled numa div do Dropdown). Execução 2: 4/5 (localizador
# de texto colidia com o rótulo neutro da tabela de preço — a tela certa, D66 nos dois campos).
# Execução 3: 4/5 (getByText pegava o <option> nativo oculto do Dropdown). Execução 4: 4/5
# (expect(page).toContainText — erro de tipo, vindo de sugestão do próprio orquestrador no
# briefing). Três correções esgotaram o limite; o usuário autorizou a quarta, de uma linha.
# Nenhuma das quatro falhas foi defeito de tela.
# gate: exit 0, 0/0/3, parser intacto; prova durável 57/57 (Sonda A com os 15 críticos; Sondas G
# e H: descricao removida -> LACUNA nominal, motivo removido -> DEFAULT_SILENCIOSO nominal).
# Menu 83 -> 84, nomeado. Recorte estrutural completo: 38 arquivos (grep de AppMenu,
# routePermissions, features/pessoas, features/clientes, ReasonDialog), 37 verdes. O vermelho,
# estoqueB41Structure.test.ts, já está corrigido na main por e645097 (b64, PR #26), que não está
# nesta cadeia de branches; o CI da PR roda sobre o merge com a main. Nas duas primeiras
# entregas o agente rodou só 9 dos 41 arquivos; o recorte completo foi pedido de volta.
# Componente/schemas: 44 verdes (22 schema, 15 página, 7 seletor no Cliente). Spec E2E: 5 casos.
# builder: typecheck, lint, validate:guard-permission-map (501 chamadas, teto 9 inalterado),
# validate:backend-contract-map (477 rotas), validate:backend-permissions verdes. Vermelhos por
# desenho declarados: guardPermissionMapMenuRules e guardPermissionMapMenuProofHistoric (menu
# 83 -> 84). validate:source vermelho por CHANGELOG (Bloco E) e pelo carimbo de
# tests/evidence/...example.json, que o briefing do orquestrador tirou do builder por engano
# (na b66 o builder o carimbava) — foi para o Bloco B.
# design: especificação no relatório do designer-ux-erp (sem arquivo). Arbitrado pelo orquestrador:
# (1) o menu proposto (filho com CLASSIFICACOES_PESSOA_GERENCIAR e pai estendido) contradizia a D68
#     e a própria página especificada, que bloqueia sem PESSOAS_CONSULTAR — prevalece a D68;
# (2) prop opcional `warning` no ReasonDialog aceito e incluído no Bloco A;
# (3) AC-6 reescrito: ações de linha ocultas pelo DataTableActions, como na b66.
nosPulados:
  - quarteto: "forma decidida na D65; molde em produção; pendências arbitradas em D68-D70"
tentativasPorNo: {}
achadosAbertos:
  - id: B-13
    dono: backend
    o_que_falta: reativação de Classificação de Pessoa
medicoes:
  - o_que: arquivos do frontend que tratam Classificação de Pessoa antes da fatia
    valor: 0
    como: "grep -rln ClassificacaoPessoa features/ — inventário 09"
decisoesNovas: [D68, D69, D70]
```

## 10. Registro final

Entrada `# v1.11.0a8b67` no `CHANGELOG.md`, com item operacional para quem administra grupos: a
tela abre com `PESSOAS_CONSULTAR` e grava com `CLASSIFICACOES_PESSOA_GERENCIAR`; quem tem só a
segunda não vê a tela (D68). Nenhum grupo perde acesso.

Aprendizado de processo: a spec E2E precisou de quatro correções, nenhuma por defeito de tela —
todas por asserção que não lia o que o PrimeReact renderiza (bloqueio do `RoutePermissionGate`,
`data-p-disabled`, `<option>` oculto) ou por sugestão errada do orquestrador no briefing
(`expect(page)`). A quarta passou do limite do nó e foi autorizada pelo usuário. Para as próximas
fatias: todo briefing de spec E2E aponta o padrão já validado da spec anterior
(`tests/e2e/b66-cliente-fornecedor.spec.ts`) para Dropdown desabilitado e rótulo neutro, e todo nó de
testes fecha com `npm run typecheck`.
